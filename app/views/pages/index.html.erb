<main>
  <section class="jumbotron text-center">
    <div class="container">
      <h1>Search for a Drive Thru</h1>
		<%= form_tag "/search", method: "get" do %>
			<div class="form-row">
				<div class="col-md-3">
					<div class="input-group mb-3">
						<div class="input-group-prepend">
							<%= label_tag :chain %>
						</div>
						<%= select_tag 'restaurant[chain]', options_for_select([
            ["McDonald's", 'mcdonalds'],
            ["Starbucks", 'starbucks'],
            ["Taco Bell", 'taco_bell'],
            ["Chick-fil-A", 'chick_fil_a'],
            ["Wendy's", 'wendys'],
            ["Burger King", 'burger_king'],
            ["Dunkin'", 'dunkin'],
            ["Panera Bread", 'panera'],
            ["Sonic Drive-in", 'sonic'],
            ["KFC", 'kfc'],
            ["Arby's", 'arbys'],
            ["Dairy Queen", 'dairy_queen'],
            ["Jack In The Box", 'jack_in_the_box'],
            ["Popeyes", 'popeyes'],
            ["Other", 'other']
            ]), class: 'form-control' %>
					</div>
				</div>
				<div class="col-md-3">
					<div class="input-group mb-3">
						<div class="input-group-prepend">
							<%= label_tag :name, "Categories" %>
						</div>
						<%= select_tag 'restaurant[name]', options_for_select(Category.all), {class: 'form-control'} %>
					</div>
				</div>
				<div class="col-md-3">
					<div class="input-group mb-3">
						<div class="input-group-prepend">
							<%= label_tag :address %>
						</div>
						<%= text_field_tag 'restaurant[address]', nil, class: 'form-control' %>
					</div>
				</div>
        <div class="col-md-3">
			    <%= submit_tag 'Find A Drive Thru' %>
        </div>
      </div>
		<% end %>
    </div>
    </section>
	</main>
	<footer> </footer>
</div>
